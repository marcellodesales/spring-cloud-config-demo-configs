sony:
  platform:
    api:
      version: v1
    payment:
      gateway:
        type: "discover"
        region: "us-west-2"


logging:
  level:
    org:
      springframework: DEBUG

# Actuators... we need /actuator/refresh
management:
  endpoints:
    web:
      exposure:
        include: "*"
