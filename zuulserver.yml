info:
  name: The Zuul API Gateway
  description: Handles all the API calls to servers using Feign Clients or Hystrix  
  version: 1.0.0

demo1:
demo2:

zuul:
  routes:
    demo1:
      serviceId: demo1
      path: /api/**
    demo2:
      serviceId: demo2
      path: /api/**

server:
  port: 8765
