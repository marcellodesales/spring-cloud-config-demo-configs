info:
  name: The Zuul API Gateway
  description: API Gateway for the Micro-services  
  version: 1.0.0

zuul:
  routes:
    demo1:
      serviceId: demo1
      path: /api/${zuul.routes.demo1.serviceId}/**
    demo2:
      serviceId: demo2
      path: /api/${zuul.routes.demo2.serviceId}/**

server:
  port: 8765
